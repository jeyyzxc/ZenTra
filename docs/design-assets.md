# Design Assets

Large non-runtime design folders such as `zentra-design/` and `zentraadmin/` are not present in this working tree.

Runtime images that are actively referenced by the application remain in `public/`. Future screenshots, mockups, wireframes, or exported design comps should live outside the production source tree, with links recorded here when needed.

